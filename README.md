# k8s_jenkins_docker_agent
 setup of Jenkins on a Minikube environment, detailing the Docker image for Jenkins agents and the necessary Kubernetes configuration files for deploying Jenkins and its agents.
